version: 2
updates:
- package-ecosystem: "pip"
  directory: "/"
  schedule:
    interval: "{{ cookiecutter.get("__dependabot_interval", "weekly") }}"
    day: "sunday"
    time: "00:00"
    timezone: "Europe/London"
